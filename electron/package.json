{
  "name": "jobsieve-desktop",
  "version": "0.0.1",
  "private": true,
  "description": "JobSieve desktop shell (Electron) embedding the NestJS app",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "electron dist/main.js",
    "rebuild-native": "electron-rebuild -f -w better-sqlite3 -m ../api"
  },
  "devDependencies": {
    "@electron/rebuild": "^3.7.0",
    "@types/node": "^24.0.0",
    "electron": "^33.0.0",
    "electron-builder": "^25.0.0",
    "typescript": "^5.7.3"
  }
}
